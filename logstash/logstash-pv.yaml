apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logstash-pvc
  namespace: bi-elk
spec:
  accessModes:
    - ReadWriteOnce  
  resources:
    requests:
      storage: 10Gi  
  storageClassName: gp2  
